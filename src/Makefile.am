bin_PROGRAMS = tabwm query_tree
tabwm_SOURCES = tabwm.cc gtk_xwindow_handler.cc
tabwm_LDADD = $(X11_LIBS) $(PANGO_LIBS) $(GDK_LIBS) $(GTK_LIBS)
tabwm_CXXFLAGS = $(X11_CFLAGS) $(PANGO_CFLAGS) $(GDK_CFLAGS) $(GTK_CFLAGS)

query_tree_SOURCES = query_tree.cc
query_tree_LDADD = $(X11_LIBS) $(PANGO_LIBS) $(GDK_LIBS) $(GTK_LIBS)
query_tree_CXXFLAGS = $(X11_CFLAGS) $(PANGO_CFLAGS) $(GDK_CFLAGS) $(GTK_CFLAGS)
